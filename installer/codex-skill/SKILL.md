# 3D AI Studio MCP Server Skill

This skill provides access to the 3D AI Studio MCP server for code generation and image tasks.

## Usage

```
@3d-ai-studio-mcp <prompt>
```

## Configuration

The skill connects to the local MCP server at `http://localhost:<port>` where `<port>` is configured during installation.
